# Web Synergium (synergium.net)

Funcionalidad para cambios en la landing WordPress de Synergium vía REST API (mismo patrón que bio-hpc.eu).

- **docs/** — esta nota
- **casos/** — cada intervención
- **wp-backups/** — JSON original antes de escribir

Último cambio en vivo (2026-08-17): pie sin el aviso de idiomas ni el `mailto` duplicado. Caso `casos/2026-08-17-quitar-aviso-idiomas/`.
