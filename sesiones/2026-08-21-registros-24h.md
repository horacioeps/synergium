# Sesión 2026-08-21 — ¿Nuevos registros Synergium últimas 24h?

## Usuario

> han habido nuevos registros en synergium en las ultimas 24 horas ?

## Agente

Consulta PocketBase `forms.synergium.net` (API + SQLite VPS + logs `_logs` + pm2 notify).

### Resumen

| Qué | Cuántos | Cuándo (UTC) | Notas |
|-----|---------|--------------|-------|
| Sync Google Sheet → PB | **26** | 2026-08-20 ~22:01–22:02 | Import masivo de filas ya en Sheet; no son altas web nuevas en ese momento |
| Alta **web** real (`nexus-input`) | **1** | 2026-08-21 **02:21:15** | **Rajarshi Mondal** — notify a horacio@horacio-ps.com |
| Altas nuevas después de eso | **0** | — | Sheet sync hoy: `created: 0`, `skipped_existing: 26` |

Pool total sigue en **29** (26 google + 1 web + 2 PublicTest).

Visitas al form hoy (GET `/api/sf/form/nexus-input`) sí hubo (tarde); sin POST de submission nuevo tras Rajarshi.

Detalle: la colección `submissions` no tiene campo `created` en schema (por eso la API no ordena por fecha); el reloj útil son los logs de POST + notify.
